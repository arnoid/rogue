package com.roguelike.world

import com.badlogic.gdx.graphics.g3d.ModelInstance

/**
 * Represents the human-controlled player.
 */
class Player(modelInstance: ModelInstance) : Actor(modelInstance) {
    override fun update(delta: Float) {
        super.update(delta)
    }
}
